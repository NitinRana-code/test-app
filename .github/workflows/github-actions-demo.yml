name: test-app-deploy
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:    
  image-build:
    runs-on: ubuntu-latest
    steps:
      - run: cd /home/runner/work/test-app/test-app
      - run: pwd
      - run: ls -la /home/runner/work/test-app/test-app
      - run: ls -la /home/runner/work/test-app
      - run: ls -la /home/runner/work/
      - run: docker build -t test-app ${{ github.workspace }}/Dockerfile
      - run: docker login -u docker4nitin -p dockertest123
      - run: docker push docker4nitin/test-app 
    
  deploy:
    runs-on: ubuntu-latest
    needs: image-build
    steps:
      - run: ssh root@168.144.66.7 -P PassWord-3478O "docker login -u docker4nitin -p dockertest123;docker pull docker4nitin/test-app;docker run test-app -p 3000:3000"